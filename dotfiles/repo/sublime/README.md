# Sublime Prefs

## Notes

- Package settings must be copied manually (for now)
- Materialize prefs must be added to the bottom of `Preferences.sublime-settings` (`CTRL + ,`) post install / manually (for now)
